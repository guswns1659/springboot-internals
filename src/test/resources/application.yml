---
spring:
  config:
    activate:
      on-profile: test

  kafka:
    jack:
      bootstrap-servers: ${spring.embedded.kafka.brokers}
      authentication:
        username: "jack"
        password: "jack"
        topics:
          jack-topic: "jack"
