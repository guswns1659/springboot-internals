---
spring:
  config:
    activate:
      on-profile: test

  kafka:
    jack:
      bootstrap-servers: "localhost:53285"
      authentication:
        username: "jack"
        password: "jack"
        topics:
          jack-topic: "jack"
